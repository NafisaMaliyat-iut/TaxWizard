<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TaxWizard | Generate Report</title>
  <link rel="icon" href="/assets/logo.png" type="image/png">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <% const inputFieldClass="rounded-lg block w-full p-4 text-lg bg-white text-[#03045e]" %>
  <% const primaryButtonClass="relative py-1.5 font-medium text-2xl rounded-xl ease-in lg:px-8 md:px-6 px-6 text-white bg-blue-400 hover:bg-gradient-to-r hover:from-[#03045e] hover:to-blue-700 hover:duration-100 hover:ease-in hover:scale-105" %>
  <% const highlightedButtonClass="relative py-1.5 font-medium text-2xl rounded-xl ease-in lg:px-6 md:px-4 px-4 text-white bg-gradient-to-r from-[#03045e] to-blue-700 hover:duration-100 hover:ease-in hover:scale-105" %>
  <% labelClass ="flex flex-start items-start uppercase text-[#03045e] text-xs font-bold mb-2" %>


  <%- include('../partials/user-sidebar.ejs') %>
  <div class="p-4 sm:py-24">
    <div class="p-4 rounded-lg ">
      <div class="grid grid-cols-1 gap-4 mb-4">
        <% if (typeof user !== 'undefined' && user) { %>
        <% console.log(user) } %>

        <div class="hover:bg-[#caf0f8] bg-[#caf0f8]/60 rounded-xl md:space-y-0 space-y-1 p-3 border-b text-center">
          <!-- <div class='bg-yellow-200 text-left p-2 my-2 flex items-center'>
            <i class='text-[#300722] text-xl mr-2 fas fa-info-circle'></i>
            <p> Please note that your taxable income is calculated based on your current profile and . Please enter your correct data.</p>
          </div> -->
          <span class="text-[#03045e] text-8xl my-10" id="taxable"></span>
          <div class="w-full mx-auto flex justify-center">
          </div>
          <div class="px-6">
            <form class="pb-2 pt-4" method="POST" id="year-data">
              <label class="<%= labelClass %>" for="year">
                Select A Year
              </label>
              <select id="year" name="year" class="<%= inputFieldClass %> origin-bottom bottom-0" required>
                <% for (let year = new Date().getFullYear(); year >= 2010; year--) { %>
                <option value="<%= year %>"><%= year %></option>
                <% } %>
              </select>
              <div class="flex mt-4 items-center">
                <button type="submit" class="<%= primaryButtonClass %>">
                  Generate
                </button>
              </div>
            </form>
          </div>
        </div>
        <section id="data" class="bg-white hidden mx-auto w-full rounded-xl shadow-xl border border-[#00b4d8]">
          <div class="p-4 border-b flex items-center justify-between">
            <h2 class="text-2xl text-[#03045e] font-bold">Tax Report</h2>
            <div class="flex items-center">
              <button type="button" onclick="" class="<%= highlightedButtonClass %>">
                <i class="fa-solid fa-download mr-2"></i>
                Download Report
              </button>
            </div>
          </div>
          <div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">Full name</p>
              <p id="full_name"></p>
            </div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">NID</p>
              <p id="nid"></p>
            </div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">Age</p>
              <p id="age"></p>
            </div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">Year</p>
              <p id="curr_year"></p>
            </div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">Yearly Income</p>
              <p id="income"></p>
            </div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">City Corporation</p>
              <p id="city_corporation"></p>
            </div>
            <div class="md:grid hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b text-[#03045e]">
              <p class="text-lg">
                We are transparent with our tax calculation. Here's a breakdown of your tax in&nbsp;<span class="font-bold" id="selected-year"></span>.
              </p>
              <br />
              <div id="breakdown-report"></div>
              <br />
              <div class="font-bold">All our tax calculations are done according to the Bangladesh Tax Act of 2023.
              </div>
            </div>

        </section>
        <section id="no-data" class="bg-white hidden mx-auto w-full rounded-xl shadow-xl border border-[#00b4d8]">
          <div>
            <div class="md:grid md:grid-cols-2 hover:bg-gray-50 md:space-y-0 space-y-1 p-4 border-b">
              <p class="text-[#03045e] text-lg">No Data Found For This Year</p>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
  <script defer>
    document.addEventListener('DOMContentLoaded', () => {
      const calculateData = document.getElementById('year-data');
      console.log("I am triggered");
      calculateData.addEventListener('submit', async (event) => {
        event.preventDefault();

        const formData = new FormData(calculateData);

        try {
          const response = await fetch('/report', {
            method: 'POST',
            body: JSON.stringify(Object.fromEntries(formData)),
            headers: {
              'Content-Type': 'application/json',
              'Authorization': 'Bearer ' + localStorage.getItem('token'),
            },
          });

          const data = await response.json();
          console.log(data);
          if (!data.taxPayer[0] || !data.user || data.taxPayer[0].length === 0 || data.user.length === 0) {
            document.getElementById('no-data').classList.remove('hidden');
          } else {
            const dataTable = document.getElementById('data');
            dataTable.classList.remove('hidden');
            document.getElementById('full_name').innerText = data.user.full_name || "N/A";
            document.getElementById('age').innerText = data.user.age || "N/A";
            document.getElementById('nid').innerText = data.user.nid || "N/A";
            document.getElementById('curr_year').innerText = data.taxPayer[0].year || "N/A";
            document.getElementById('income').innerText = data.taxPayer[0].yearly_amount || "N/A";
            document.getElementById('city_corporation').innerText = data.user.city_corporation || "N/A";
            document.getElementById('selected-year').innerText = data.taxPayer[0].year || "N/A";
          }
          calculateTax(data.taxPayer[0].yearly_amount, data.user.gender, data.user.age, data.user.city_corporation);
        } catch (error) {
          console.log(error);
        }
      });
    });
  </script>
  <script>
    function calculateTax(income, gender, age, city_corporation) {
      const taxFreeIncome = gender === "female" || age > 65 ? 400000 : 350000;
      document.getElementById('breakdown-report').innerHTML += `<li>Your tax-free income (the amount you can earn without having to pay for any taxes) is BDT ${taxFreeIncome}.</li>`;

      income = parseFloat(income);
      let taxableIncome = income - taxFreeIncome;

      if (taxableIncome <= 0) {
        document.getElementById('breakdown-report').innerHTML += `<li>Since your yearly income BDT ${income} did not exceed the tax-free income, your taxable amount for this year is at <strong>BDT 0</strong>.</li>`;
        return;
      }

      document.getElementById('breakdown-report').innerHTML += `<li>Your yearly income exceeds the tax-free income, your taxable amount for this year is at BDT ${taxableIncome}.</li>`;

      let totalTax = 0;

      if (taxableIncome <= 100000) {
        totalTax = taxableIncome * 0.05;
        document.getElementById('breakdown-report').innerHTML += `<li>Your taxable amount BDT ${taxableIncome} exceeds the BDT 100,000 threshold, so your total tax is 5% of your taxable income. In your case, this is BDT ${totalTax}.</li>`;
      } else {
        totalTax += 100000 * 0.05;
        document.getElementById('breakdown-report').innerHTML += `<li>Your taxable amount BDT ${taxableIncome} reaches the BDT 100,000 threshold, 5% of it is added to tax. In your case, this is BDT ${0.05*taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
        taxableIncome -= 100000;

        if (taxableIncome <= 300000) {
          totalTax += taxableIncome * 0.1;
          document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 300,000 threshold, so 10% of it is added to the tax. In your case, this is BDT ${0.1 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
        } else {
          totalTax += 300000 * 0.1;
          document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 300,000 threshold, so 10% of it is added to the tax. In your case, this is BDT ${0.1 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
          taxableIncome -= 300000;

          if (taxableIncome <= 400000) {
            totalTax += taxableIncome * 0.15;
            document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 400,000 threshold, so 15% tax is added to it. In your case, this is BDT ${0.15 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
          } else {
            totalTax += 400000 * 0.15;
            document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 400,000 threshold, and 15% tax is added to it. In your case, this is BDT ${0.15 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
            taxableIncome -= 400000;

            if (taxableIncome <= 500000) {
              totalTax += taxableIncome * 0.2;
              document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 500,000 threshold, and 20% tax is added to it. In your case, this is BDT ${0.20 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
            } else {
              totalTax += 500000 * 0.2;
              taxableIncome -= 500000;
              document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 500,000 threshold, and 20% tax is added to it. In your case, this is BDT ${0.20 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li>`;
              // Calculate the remaining tax at 25%
              if (taxableIncome > 1650000) {
                totalTax += taxableIncome * 0.25;
                document.getElementById('breakdown-report').innerHTML += `<li>You have an additional BDT ${taxableIncome} taxable income which exceeds the BDT 1,650,000 threshold, and 25% tax is added to it. In your case, this is BDT ${0.25 * taxableIncome}. Your total tax now stands at BDT ${totalTax}.</li> `;
              }
            }
          }
        }
      }

      // Check if the user is eligible for the minimum tax
      const minimumTax =
        city_corporation === "dhaka" || city_corporation === "chattogram" ? 5000 :
        city_corporation === "othercity" ? 4000 :
        3000;

      // Apply minimum tax if applicable
      res = Math.max(minimumTax, totalTax);
      if(res!== totalTax){
      document.getElementById('breakdown-report').innerHTML += `<li>Based on your Corporation, you are subject to a minimum tax of BDT ${minimumTax}. Even though your taxable income is BDT ${taxableIncome}, the minimum tax for your region is BDT ${minimumTax}. Therefore, your total tax for this year is BDT ${minimumTax}.</li>`;

      }
      document.getElementById('breakdown-report').innerHTML += `Your total tax for this year comes to BDT ${totalTax}.`;

      return totalTax;
    }
  </script>
</body>

</html>